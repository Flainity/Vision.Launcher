<Window x:Class="Application.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Application.Launcher"
        xmlns:view="clr-namespace:Application.Launcher.View"
        xmlns:viewmodel="clr-namespace:Application.Launcher.ViewModel"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="Vision of the Past"
        Height="700"
        Width="1200"
        WindowStartupLocation="CenterScreen" 
        AllowsTransparency="True" 
        Background="Transparent" 
        WindowStyle="None" 
        ResizeMode="NoResize" 
        d:DataContext="{d:DesignInstance viewmodel:MainWindowViewModel}">
    
    <Border CornerRadius="9" BorderBrush="#444" BorderThickness="1" Margin="20">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#262529" Offset="0"/>
                <GradientStop Color="#251f47" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Opacity=".5" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="4*"/>
            </Grid.ColumnDefinitions>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="40px"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Grid.Row="1" Grid.Column="0" 
                    CornerRadius="0,0,0,9" 
                    Background="#24282f"
                    ClipToBounds="True">
                <view:SidebarView/>
            </Border>
            <view:TopBarView Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="5,0"/>
            
            <Border Grid.Row="1" Grid.Column="1" CornerRadius="0,8,8,0" BorderThickness="1,0,0,0" BorderBrush="#171d25">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#262529" Offset="0"/>
                        <GradientStop Color="#251f47" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="100"/>
                    </Grid.RowDefinitions>

                    <ContentControl Content="{Binding CurrentView}"/>
                    <ContentControl Grid.Row="1" Content="{Binding PatchView}" Margin="10,0"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
