<UserControl x:Class="Application.Launcher.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Application.Launcher.View"
             xmlns:viewmodel="clr-namespace:Application.Launcher.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600" Background="#222">
    <UserControl.DataContext>
        <viewmodel:SettingsViewModel/>
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourceDictionary/General/ComboBox.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <Label Grid.Column="0" Grid.Row="0" Content="Resolution" FontSize="14" Foreground="White"/>
        <Label Grid.Column="1" Grid.Row="0" Content="Something else" FontSize="14" Foreground="White"/>
        
        <TextBox Grid.Column="0" Grid.Row="1" Margin="5" IsEnabled="False" Text="{Binding VideoSettings.X}"/>
        <ComboBox Grid.Column="1" ItemsSource="{Binding PossibleResolutions}" Grid.Row="1" Margin="5"/>
    </Grid>
</UserControl>
