<Window x:Class="Application.Launcher.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Application.Launcher.Windows"
        xmlns:viewmodel="clr-namespace:Application.Launcher.ViewModel.Windows"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="Sign in" 
        Height="410" 
        Width="300" 
        WindowStartupLocation="CenterScreen" 
        AllowsTransparency="True" 
        Background="Transparent" 
        WindowStyle="None" 
        ResizeMode="NoResize" 
        d:DataContext="{d:DesignInstance viewmodel:LoginWindowViewModel}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourceDictionary/General/Button.xaml"/>
                <ResourceDictionary Source="../ResourceDictionary/General/ProgressBar.xaml"/>
                <ResourceDictionary Source="../ResourceDictionary/General/TextBox.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border CornerRadius="10" BorderThickness="1" BorderBrush="#444" Margin="10" MouseDown="UIElement_OnMouseDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Opacity=".5" ShadowDepth="0"/>
        </Border.Effect>
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#262529" Offset="0"/>
                <GradientStop Color="#251f47" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="160"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Image Source="../Resource/Image/VisionLogo-Small.png" Margin="15"/>

            <StackPanel Grid.Row="1" Orientation="Vertical">
                <Label Content="Username" Margin="20, 5" Foreground="White" HorizontalContentAlignment="Center"/>
                <TextBox Height="25" Margin="20, 0" Text="{Binding Username}" VerticalContentAlignment="Center" Padding="3,0,0,0"/>
                <Label Content="Password" Margin="20, 5" Foreground="White" HorizontalContentAlignment="Center"/>
                <TextBox Height="25" Margin="20, 0" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" Padding="3,0,0,0" KeyUp="UIElement_OnKeyUp"/>
                <CheckBox Content="Stay signed in" Margin="20,15" Foreground="White"/>
                <Button Height="40" Margin="20,0,20, 20" Style="{StaticResource StartButton}" Content="Login" Command="{Binding RequestLoginCommand}"/>
            </StackPanel>
        
            <Border Grid.Row="0" Grid.RowSpan="2" Background="#000" Opacity=".5" Visibility="{Binding IsLoginProcessRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <fa:ImageAwesome Grid.Row="0" Grid.RowSpan="2" Icon="Spinner" Spin="True" Width="30" Foreground="White" Visibility="{Binding IsLoginProcessRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </Grid>
    </Border>
</Window>
