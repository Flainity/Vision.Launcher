<UserControl x:Class="Veritas.Services.Launcher.View.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Veritas.Services.Launcher.View"
             xmlns:viewmodel="clr-namespace:Veritas.Services.Launcher.ViewModel"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:fav="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="250" Background="#222">
    <UserControl.DataContext>
        <viewmodel:SidebarViewModel/>
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../ResourceDictionary/Sidebar/ListBox.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Border CornerRadius="0" Background="Transparent">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="150"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        
            <Image Grid.Row="0" Source="../Resource/Image/VisionLogo-Small.png"/>
            <ListBox Grid.Row="1" x:Name="Navigation" ItemsSource="{Binding NavigationItems}" SelectedItem="{Binding SelectedItem}" Background="#222" BorderThickness="0" SelectionMode="Single" Padding="10">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                        
                            <fav:ImageAwesome Grid.Column="0" Icon="{Binding Icon}" Foreground="Gray" Margin="5"/>
                            <Label Grid.Column="1" Content="{Binding Name}" Foreground="DarkGray" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="14" FontWeight="Bold" Margin="10"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
