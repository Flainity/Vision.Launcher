<Window x:Class="Veritas.Services.Launcher.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Veritas.Services.Launcher.Windows"
        xmlns:viewmodel="clr-namespace:Veritas.Services.Launcher.ViewModel.Windows"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        mc:Ignorable="d"
        Title="Sign in" Height="500" Width="300" WindowStartupLocation="CenterScreen" WindowStyle="None" d:DataContext="{d:DesignInstance viewmodel:LoginWindowViewModel}">
    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label FontSize="24" Content="Login" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Label Content="Username" Margin="20, 5"/>
            <TextBox Height="25" Margin="20, 0" Text="{Binding Username}"/>
            <Label Content="Password" Margin="20, 5"/>
            <TextBox Height="25" Margin="20, 0" Text="{Binding Password}"/>
            <Button Height="25" Margin="20" Content="Login" Command="{Binding RequestLoginCommand}"/>
        </StackPanel>
        
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#000" Opacity=".5" Visibility="{Binding IsLoginProcessRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <fa:ImageAwesome Grid.Row="0" Grid.RowSpan="3" Icon="Spinner" Spin="True" Width="30" Foreground="White" Visibility="{Binding IsLoginProcessRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Window>
